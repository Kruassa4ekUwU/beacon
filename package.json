{
  "name": "beacon-app",
  "version": "1.0.0",
  "private": true,
  "description": "Beacon — P2P file transfer, wrapped for Android",
  "scripts": {
    "cap:add": "cap add android",
    "cap:sync": "cap sync android"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/android": "^6.1.2",
    "capacitor-app-extractor": "file:./plugins/capacitor-app-extractor"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
